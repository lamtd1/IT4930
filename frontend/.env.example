# Base URL of the FastAPI backend (PRD §7). Example: http://localhost:8000
# When unset, the app runs in DEMO mode using a local mock catalog.
# To use the real backend, copy this file to `.env` and set the URL:
VITE_API_BASE=
